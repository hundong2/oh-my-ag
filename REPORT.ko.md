# 저장소 구성 및 .agent 생태계 분석 보고서

이 보고서는 현재 저장소의 설정 파일, 특히 `.agent` 폴더와 그 내부 구조, 그리고 `skill` 폴더 및 기타 설정 파일들의 역할에 대해 상세히 분석한 내용을 담고 있습니다.

## 1. `.agent` 폴더 개요

`.agent` 폴더는 이 프로젝트의 **"두뇌"**이자 **"운영 매뉴얼"** 저장소입니다. AI 에이전트가 이 프로젝트 내에서 어떻게 행동해야 하는지, 어떤 규칙을 따라야 하는지, 그리고 어떤 도구를 사용할 수 있는지를 정의합니다.

### 주요 구성 요소

| 경로 | 설명 |
|---|---|
| `.agent/config/` | 사용자 환경 설정이 위치합니다. (`user-preferences.yaml`) |
| `.agent/skills/` | 특화된 에이전트(Skill)들의 정의와 가이드라인이 모여 있습니다. |
| `.agent/workflows/` | 표준화된 작업 절차(Workflow)들이 정의되어 있습니다. |
| `.agent/mcp.json` | MCP(Model Context Protocol) 서버 설정 파일로, AI가 사용할 도구와 메모리를 연결합니다. |

---

## 2. 상세 구조 분석

### 2.1. Skills (전문 기술/에이전트)
**경로:** `.agent/skills/`

이 폴더는 AI가 수행할 수 있는 구체적인 **역할(Role)**들을 정의합니다. 각 하위 폴더(예: `backend-agent`, `frontend-agent`)는 하나의 전문 분야를 나타냅니다.

*   **`SKILL.md`**: 각 스킬 폴더의 핵심 파일입니다.
    *   **역할 정의**: 언제 이 에이전트를 사용해야 하는지(`When to use`), 언제 사용하지 말아야 하는지(`When NOT to use`)를 명시합니다.
    *   **아키텍처 규칙**: 해당 분야의 코드 구조(예: Router -> Service -> Repository)를 강제합니다.
    *   **코딩 표준**: 명명 규칙, 라이브러리 사용 원칙, 코드 품질 기준 등을 정의합니다.
    *   **실행 가이드**: 작업을 수행할 때 따라야 할 구체적인 프로세스를 참조합니다.
*   **`_shared/`**: 모든 에이전트가 공통으로 공유하는 프로토콜(메모리 관리, 컨텍스트 로딩 등)이 위치합니다.

### 2.2. Workflows (작업 흐름)
**경로:** `.agent/workflows/`

반복적이고 복잡한 작업을 AI가 일관되게 수행할 수 있도록 단계별로 정리한 **절차서**입니다.

*   예: `setup.md` (초기 설정), `plan.md` (기획), `debug.md` (디버깅)
*   각 파일은 AI에게 "1단계에서 이것을 확인하고, 2단계에서 저것을 실행하라"는 식의 명확한 지침을 제공합니다. 이는 AI의 환각(Hallucination)을 줄이고 작업 품질을 표준화하는 데 기여합니다.

### 2.3. Config (설정)
**경로:** `.agent/config/`

*   **`user-preferences.yaml`**: 사용자의 선호 사항을 저장합니다.
    *   `language`: AI 응답 언어 (예: `ko`, `en`)
    *   `timezone`: 시간대 설정
    *   `agent_cli_mapping`: 특정 작업을 어떤 AI 모델(CLI)에게 맡길지 지정 (예: 프론트엔드는 Gemini, 백엔드는 Claude 등)

### 2.4. MCP (Model Context Protocol)
**파일:** `.agent/mcp.json`

AI 모델과 외부 시스템(이 경우 로컬 개발 환경)을 연결하는 설정입니다.
*   **`serena`**: 이 프로젝트에서 사용하는 MCP 서버의 이름으로 보입니다.
*   **Tools**: 파일 탐색, 코드 분석, 메모리 읽기/쓰기 등 AI가 실제로 실행할 수 있는 기능들을 활성화합니다.

---

## 3. `.gitkeep` 및 기타 파일의 역할

### 3.1. `.gitkeep`
> **현재 상태**: 분석 결과, 현재 이 저장소의 `.agent` 폴더나 상위 디렉토리에서 `.gitkeep` 파일은 발견되지 않았습니다. 하지만 일반적인 역할에 대해 설명해 드립니다.

*   **역할**: Git은 기본적으로 **빈 폴더(Empty Directory)**를 추적하지 않습니다. 폴더 구조는 유지하고 싶지만 안에 아직 파일이 없는 경우, 임의의 파일인 `.gitkeep`을 생성하여 Git이 해당 폴더를 저장소에 포함시키도록 강제하는 용도로 사용됩니다.
*   **이름**: 공식적인 Git 명령어가 아니며, 관습적으로 사용하는 이름입니다.

### 3.2. 기타 주요 파일
*   **`REPORT.ko.md` / `REPORT.md`**: (현재 생성 중) 사용자의 요청에 따라 생성된 분석 보고서 파일입니다.

---

## 요약

이 저장소는 **"Agentic Workflow"**를 강력하게 지원하도록 설계되어 있습니다.
1.  **`.agent/skills`**는 AI에게 **"전문가적 지식과 태도"**를 부여합니다.
2.  **`.agent/workflows`**는 AI에게 **"일하는 순서"**를 가르칩니다.
3.  **`.agent/mcp.json`**은 AI에게 **"작업할 수 있는 손발(도구)"**을 제공합니다.
4.  **`.agent/config`**는 사용자의 **"취향"**에 맞게 이들을 조율합니다.

이러한 구조를 통해 AI는 단순한 코딩 보조를 넘어, 기획자, 개발자, QA의 역할을 명확한 룰과 프로세스 안에서 수행할 수 있게 됩니다.
